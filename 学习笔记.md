# AI 学习第一周 — 学习笔记

> 日期：昨天（2026-06-20）

---

## 目录

1. [config.py — 项目配置管理](#1-configpy--项目配置管理)
2. [models/schemas.py — Pydantic 数据模型](#2-modelsschemaspy--pydantic-数据模型)
3. [utils/document_parser.py — 文档解析](#3-utilsdocument_parserpy--文档解析)
4. [utils/text_splitter.py — 文本分块](#4-utilstext_splitterpy--文本分块)
5. [services/document_service.py — 文档入库服务](#5-servicesdocument_servicepy--文档入库服务)
6. [services/memory_service.py — 对话记忆管理](#6-servicesmemory_servicepy--对话记忆管理)
7. [services/rag_service.py — RAG 检索与问答](#7-servicesragservicepy--rag-检索与问答)

---

## 1. config.py — 项目配置管理

### 核心知识点

**什么是 python-dotenv？**
- 读取 `.env` 文件中的键值对，加载到 `os.environ` 中
- 本地开发时 API Key 写在 `.env` 文件里（不提交到 Git）
- 部署时通过系统环境变量传入，代码不用改

**os.getenv("KEY", "default") 的含义：**
- 先从环境变量找 KEY，找不到就用 default 值
- 即使没有 `.env` 文件，程序也能用默认值跑起来

### 关键概念解释

| 概念 | 说明 |
|------|------|
| **持久化（Persistent）** | 数据保存在磁盘上，程序重启后数据不丢失 |
| **Embedding** | 将文本转换为一组数字向量，语义相近的文本向量也相近 |
| **Chunk Size** | 每个文本块的最大字符数，太大不精准，太小语义不完整 |
| **Chunk Overlap** | 相邻块之间的重叠字符数，避免关键信息在边界被截断 |

### 重要代码片段

```python
import os
from pathlib import Path
from dotenv import load_dotenv

load_dotenv()  # 加载 .env 文件

OPENAI_API_KEY = os.getenv("OPENAI_API_KEY", "")  # 带默认值的读取

# 启动时自动创建必要目录
Path(UPLOAD_DIR).mkdir(parents=True, exist_ok=True)
```

### 个人理解与总结

配置文件的核心思想是**关注点分离**——把"会变的参数"和"不变的逻辑"分开。这样改配置不需要改代码，也避免了敏感信息泄露的风险。`python-dotenv` + `os.getenv` 是 Python 项目中最常见的配置方案。

---

## 2. models/schemas.py — Pydantic 数据模型

### 核心知识点

**什么是 Pydantic？**
- Python 的数据验证库，FastAPI 的核心依赖
- 继承 `BaseModel`，Pydantic 自动完成：
  - 类型验证（类型不对就报错）
  - 序列化/反序列化（Python 对象 ↔ JSON）
  - 生成 JSON Schema（用于 API 文档）

**为什么需要数据模型？**
- 前端传来的数据可能格式不对
- Pydantic 在入口处拦截错误
- 看模型就知道接口需要什么、返回什么

### 关键概念解释

| 概念 | 说明 |
|------|------|
| **BaseModel** | Pydantic 的基类，所有模型继承它 |
| **Field()** | 定义字段属性（描述、默认值、验证规则） |
| **Optional[str] = None** | 可选字段，不传时默认为 None |
| **response_model** | FastAPI 用它验证返回数据并生成文档 |

### 重要代码片段

```python
from pydantic import BaseModel, Field
from typing import Optional

class ChatRequest(BaseModel):
    question: str = Field(..., description="用户的问题")          # 必填
    session_id: Optional[str] = Field(None, description="会话ID")  # 可选

class UploadResponse(BaseModel):
    filename: str
    chunks_count: int
    message: str
```

### 个人理解与总结

Pydantic 模型相当于给数据加了"安检门"，不合格的数据进不来。在 FastAPI 中，请求模型自动解析请求体，响应模型自动验证返回值并生成 OpenAPI 文档，一举多得。

---

## 3. utils/document_parser.py — 文档解析

### 核心知识点

**为什么需要文档解析？**
- 用户上传的是"人读"的格式（PDF 排版、Word 样式）
- RAG 系统需要纯文本，所以要把各种格式统一转成纯文本

**设计模式：策略模式**
- `parse_document()` 是统一入口，根据扩展名选择解析器
- 新增格式只需加一个函数和一行映射

### 关键概念解释

| 解析器 | 库 | 原理 |
|--------|-----|------|
| PDF | pypdf | PdfReader 逐页 extract_text() |
| Word | python-docx | Document.paragraphs 提取段落 |
| Markdown | 内置 | 直接读取，保留格式符号 |
| TXT | 内置 | 最简单，直接读取 |

### 重要代码片段

```python
# 解析器映射表（策略模式核心）
PARSERS = {
    "pdf": parse_pdf,
    "docx": parse_docx,
    "md": parse_markdown,
    "txt": parse_txt,
}

def parse_document(file_path: str) -> str:
    ext = Path(file_path).suffix.lstrip(".").lower()
    parser = PARSERS.get(ext)
    if not parser:
        raise ValueError(f"不支持的格式: .{ext}")
    return parser(file_path)
```

### 学习中遇到的问题及解决方案

| 问题 | 解决方案 |
|------|----------|
| 扫描版 PDF 无法提取文字 | 需要 OCR 技术（本项目暂未处理） |
| Word 表格中的文字丢失 | python-docx 需要额外处理 tables 属性 |
| 编码问题导致读取失败 | 统一使用 utf-8 编码 |

---

## 4. utils/text_splitter.py — 文本分块

### 核心知识点

**为什么要分块？**
- LLM 有上下文长度限制，整篇文档太长不能全部塞进去
- 向量检索也需要小块：块越小检索越精准

**RecursiveCharacterTextSplitter 工作原理**
- "递归"：先尝试按第一个分隔符切，切出来还是太长就换下一个
- 分隔符优先级：`\n\n` > `\n` > `。`/`.` > 空格 > 字符
- 尽量在语义边界处切分

### 关键概念解释

| 参数 | 推荐值 | 含义 |
|------|--------|------|
| chunk_size | 500（中文） | 每块最大字符数 |
| chunk_overlap | 50 | 相邻块重叠字符数（10%-20% of size） |

### 重要代码片段

```python
splitter = RecursiveCharacterTextSplitter(
    chunk_size=500,
    chunk_overlap=50,
    separators=[
        "\n\n",   # 段落边界（优先）
        "\n",     # 行边界
        "。",     # 中文句号
        ".",      # 英文句号
        "",       # 最后手段：按字符切
    ],
)
chunks = splitter.split_text(text)
```

### 个人理解与总结

分块策略是一个权衡：小块检索精准但上下文可能不完整，大块上下文完整但可能混入无关内容。实际项目中需要根据数据特点调优。中文建议 chunk_size 设为 300-600，因为中文字符信息密度更高。

---

## 5. services/document_service.py — 文档入库服务

### 核心知识点

**RAG 入库完整流程：**
上传 → 格式校验 → 保存文件 → 解析为文本 → 分块 → 向量化 → 存入 Chroma

**ChromaDB 核心概念：**

| 概念 | 类比 |
|------|------|
| Collection | 数据库的"表" |
| Document | 一条记录（原始文本） |
| Embedding | 向量表示 |
| Metadata | 元数据（来源文件名等） |

**get_or_create_collection 的意义：**
- 第一次运行创建 Collection
- 后续运行获取已有的（不会重复创建）
- 已入库的文档不会丢失

### 关键概念解释

| 概念 | 说明 |
|------|------|
| **PersistentClient** | 数据持久化到磁盘，重启不丢 |
| **HNSW** | 一种高效的向量索引算法 |
| **余弦相似度（cosine）** | 衡量向量方向相似性，适合文本语义 |
| **余弦距离** | distance = 1 - similarity，越小越相似 |

### 重要代码片段

```python
collection = client.get_or_create_collection(
    name=CHROMA_COLLECTION_NAME,
    metadata={"hnsw:space": "cosine"},
)

# UUID 生成唯一 ID
ids = [f"{uuid.uuid4().hex}" for _ in chunks_with_meta]

collection.add(
    documents=texts,
    embeddings=embeddings,       # 或省略让 Chroma 自动生成
    metadatas=metadatas,
    ids=ids,
)
```

### 学习中遇到的问题及解决方案

| 问题 | 解决方案 |
|------|----------|
| Embedding 调用失败 | 设计了双模式：有 API Key 用 OpenAI，否则用 Chroma 默认模型 |
| 同名文件覆盖 | 用 UUID 生成唯一文件名前缀 |
| 解析失败残留文件 | try-except 中用 unlink() 删除已保存的文件 |

---

## 6. services/memory_service.py — 对话记忆管理

### 核心知识点

**为什么需要对话记忆？**
- 没有记忆：每次提问都是全新的，AI 不知道之前聊了什么
- 有记忆：能理解"它指的是什么"、"继续上面的分析"
- 这是多轮对话的基础

**当前实现 vs 生产环境：**

| 维度 | 当前实现（内存 dict） | 生产环境推荐 |
|------|---------------------|-------------|
| 存储 | `_conversation_store` 字典 | Redis / PostgreSQL |
| 持久性 | 重启丢失 | 持久化存储 |
| 性能 | 单机够用 | 分布式支持 |
| 复杂度 | 极简 | 需要连接池等 |

### 关键概念解释

| 概念 | 说明 |
|------|------|
| **session_id** | 区分不同对话的标识，不同用户/话题隔离 |
| **裁剪策略** | 只保留最近 N 条，避免 Token 超限 |
| **MAX_HISTORY_LENGTH** | 本项目设为 20 条 |

### 重要代码片段

```python
_conversation_store: dict[str, list[dict]] = {}

def add_to_history(session_id: str, role: str, content: str) -> None:
    if session_id not in _conversation_store:
        _conversation_store[session_id] = []
    _conversation_store[session_id].append({"role": role, "content": content})
    # 裁剪：只保留最近 MAX_HISTORY_LENGTH 条
    if len(_conversation_store[session_id]) > MAX_HISTORY_LENGTH:
        _conversation_store[session_id] = _conversation_store[session_id][-MAX_HISTORY_LENGTH:]
```

### 个人理解与总结

内存存储适合开发阶段快速验证逻辑，但生产环境必须换成 Redis 或数据库。裁剪策略也很重要——LLM 有 Token 上限，不能无限制地塞历史消息。

---

## 7. services/rag_service.py — RAG 检索与问答

### 核心知识点

**RAG 完整流程（后半段）：**
问题 → 向量化 → Chroma 检索最相似文档 → 构建 Prompt → LLM 生成回答

**Prompt 模板的作用：**
- 给 LLM 的"指令模板"，里面有占位符 `{context}`、`{question}`
- 运行时替换占位符，生成完整的 Prompt
- 好的 Prompt 是 RAG 效果的关键

**为什么标注来源？**
- RAG 和普通聊天的核心区别：回答基于文档，不是瞎编
- 标注来源让用户能验证答案可靠性

### 关键概念解释

| 概念 | 说明 |
|------|------|
| **SystemMessage** | 设定 AI 角色和行为规则 |
| **HumanMessage** | 用户消息 |
| **AIMessage** | AI 回复（用于历史） |
| **temperature** | 控制回答随机性，0.3 = 更确定性，减少编造 |
| **余弦距离→相似度** | `score = round(1 - distance, 4)` |

### 重要代码片段

```python
# RAG Prompt 模板
RAG_PROMPT_TEMPLATE = """你是一个专业的知识库问答助手...
参考文档：{context}
用户问题：{question}
请给出回答："""

# 检索结果整理（距离转相似度）
retrieved.append({
    "text": doc,
    "source": metadata.get("source", "未知来源"),
    "chunk_index": metadata.get("chunk_index", 0),
    "relevance_score": round(1 - distance, 4),  # 余弦距离 → 相似度分数
})

# 对话历史注入（只取最近6条）
if history:
    for msg in history[-6:]:
        if msg["role"] == "user":
            messages.append(HumanMessage(content=msg["content"]))
        elif msg["role"] == "assistant":
            messages.append(AIMessage(content=msg["content"]))
```

### 学习中遇到的问题及解决方案

| 问题 | 解决方案 |
|------|----------|
| 向量库为空时查询报错 | 先检查 collection.count() == 0 |
| 没有 OpenAI Key 怎么办 | 让 Chroma 自动用默认模型生成向量 |
| 历史太长超 Token | 只取最近 6 条历史消息 |
| 回答可能编造信息 | Prompt 中明确要求"只根据文档回答" |

### 个人理解与总结

RAG 的精髓在于"检索增强"——不是让 LLM 凭空回答，而是先找到相关资料再基于资料回答。这使得回答更准确、可追溯。整个流程中最值得调优的是：分块策略、Embedding 模型选择、Prompt 模板设计和 top_k 取值。

---

## 总结

本周学习的 RAG 系统涵盖了现代 AI 应用开发的核心技能：

1. **工程实践**：配置管理、数据验证、错误处理
2. **AI 基础**：Embedding、向量数据库、相似度计算
3. **框架使用**：FastAPI、LangChain、ChromaDB、Pydantic
4. **架构思想**：策略模式、关注点分离、幂等操作

下一步重点：实际运行项目进行端到端测试，深入 LangChain 消息体系，学习生产级优化。
